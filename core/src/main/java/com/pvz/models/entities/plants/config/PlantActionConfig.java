package com.pvz.models.entities.plants.config;

public abstract class PlantActionConfig {

    /** PAM clip label this action plays while active (its own attack / plant-food animation). */
    public String label;
}
