package Controller;

import View.Result;
import java.util.regex.Matcher;

public class NetworkController {
    public static Result connect(Matcher matcher) { return null; }
    public static Result back(Matcher matcher) { return null; }
}
