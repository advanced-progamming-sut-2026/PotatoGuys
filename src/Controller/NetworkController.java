package Controller;

import View.Result;
import java.util.regex.Matcher;

public class NetworkController {
    public Result connect(Matcher matcher) { return null; }
    public Result back(Matcher matcher) { return null; }
}
